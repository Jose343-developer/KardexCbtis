version: '3.8'

services:
  db:
    image: mcr.microsoft.com/azure-sql-edge:latest
    container_name: DockerCbtis
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=P@ssw0rd2026!
    ports:
      - "1435:1433"
    volumes:
      - ./DbData:/var/opt/mssql/data
